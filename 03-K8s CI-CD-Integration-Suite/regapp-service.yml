# apiVersion: v1
# kind: Service
# metadata:
#   name: valaxy-service
#   labels:
#     app: regapp 
# spec:
#   selector:
#     app: regapp 

#   ports:
#     - port: 8080
#       targetPort: 8080

#   type: LoadBalancer


apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  labels:
    app: myapp
spec:
  selector:
    app: myapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30080 # Specify a custom NodePort (optional, or let Kubernetes assign one automatically)
  type: NodePort

